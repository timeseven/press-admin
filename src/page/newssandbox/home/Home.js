import { Button } from "antd";

const Home = () => {
  const getData = () => {};
  return (
    <div>
      <Button type="primary" onClick={getData}>
        PRESS ME
      </Button>
    </div>
  );
};

export default Home;
